[package]
name = "wc-core"
version = "2026.3.12-3"
edition.workspace = true
license.workspace = true

[dependencies]
chrono = { version = "0.4", default-features = true, features = ["clock"] }
anyhow = "1.0"
