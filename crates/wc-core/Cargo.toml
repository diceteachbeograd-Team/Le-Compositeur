[package]
name = "wc-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
chrono = { version = "0.4", default-features = true, features = ["clock"] }
anyhow = "1.0"
