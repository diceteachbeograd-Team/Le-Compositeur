# Test Matrix (Draft)

## Zielsysteme
1. Fedora Workstation (GNOME, Wayland)
2. Fedora KDE Spin (Wayland)
3. Ubuntu LTS (GNOME, Wayland)
4. Debian oder openSUSE

## Checkliste pro Lauf
- Build erfolgreich
- `wc-cli doctor` läuft
- `wc-cli render-preview` läuft
- (später) Wallpaper-Setzen funktioniert

