# Release Staging 0.9.0

Drop build artifacts for version `0.9.0` here before publishing.

Expected targets:
- `linux-rpm/` (`.rpm`)
- `linux-deb/` (`.deb`)
- `windows-x86_64/` (`.zip` or installer)
- `macos-aarch64/` (`.tar.gz` or `.dmg`)
- `macos-x86_64/` (`.tar.gz` or `.dmg`)

Note:
- Keep checksums and release notes next to artifacts for traceability.
